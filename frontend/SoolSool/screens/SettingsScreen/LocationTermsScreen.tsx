import { WebView } from "react-native-webview";

const LocationTermsScreen = () => {
  return (
    <WebView
      source={{
        uri: "https://",
      }}
      style={{ flex: 1 }}
    />
  );
};

export default LocationTermsScreen;
